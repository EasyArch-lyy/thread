伪共享：
Cache(解决CPU和内存速度差问题的高速缓存，在CPU与主内存之间)内部按行存储，一行为2的幂次数字节
CPU先去cache查看是否有需要的变量，没用才去主内存。从主内存中取过后，会把该内存区域一个cache行大小内存
赋值到cache中，存放到cache行中是内存块，会有多个变量在一个缓存行，同时只能有一个变量操作缓存行，
相比每个变量放入一个缓存行，此前行为导致性能下降

避免伪共享：
创建变量时填充字段
